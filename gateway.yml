zuul:
  ignoredServices: configserver, discovery, gateway
  sensitiveHeaders: Cookie,Set-Cookie

security:
  ignored: /auth/oauth/**
  oauth2.resource:
    user-info-uri: http://auth/me
    load-balanced: true
